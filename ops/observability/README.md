# Observability Assets

- `dashboard_accel_rust.json`: Grafana dashboard template for Rust core.
- `alerts_accel_rust.yml`: Prometheus alert rules (latency/error/quota/task-store/routing fallback).

These files are environment-agnostic templates. Bind data source and labels in your deployment.
